## 1. 实现数据存在性检查
- [ ] 1.1 修改 `useCostLoader` hook，在发起请求前检查 store 中是否已有对应租户和周期的数据
- [ ] 1.2 如果数据已存在，跳过请求并直接返回（不设置 loading 状态）
- [ ] 1.3 仅在数据不存在时才发起 API 请求

## 2. 实现请求去重机制
- [ ] 2.1 使用 `useRef` 跟踪正在进行的请求，避免同一周期数据的并发请求
- [ ] 2.2 如果已有相同租户和周期的请求正在进行，复用该请求而不是发起新请求

## 3. 处理用户主动切换周期
- [ ] 3.1 确保用户切换周期时（通过 Tabs 组件）仍会重新获取最新数据
- [ ] 3.2 验证切换周期时的数据加载行为符合预期

## 4. 验证修复效果
- [ ] 4.1 测试展开「模型消耗」AccordionPanel 时，确认只发起一个请求
- [ ] 4.2 测试切换周期时，确认会重新获取对应周期的数据
- [ ] 4.3 测试手动刷新时，确认会重新获取最新数据
- [ ] 4.4 运行 `bun run build` 确认编译通过
