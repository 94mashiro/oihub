## 1. Implementation
- [x] 1.1 创建 `lib/utils/ccswitch-deeplink.ts` 工具函数，封装 deeplink URL 构建逻辑
- [x] 1.2 在 `token-list-panel.tsx` 中添加导出按钮和应用选择 Popover
- [x] 1.3 实现 deeplink 跳转逻辑（`window.open` 或 `location.href`）

## 2. Validation
- [x] 2.1 运行 `bun run compile` 确认类型检查通过
- [x] 2.2 运行 `bun run build` 确认打包成功
